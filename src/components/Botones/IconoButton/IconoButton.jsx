import React from 'react'
import IconButton from '@mui/material/IconButton';

export const IconoButton = ({icono}) => {
  return (
    <IconButton aria-label="linkedin" color='secondary'>
        {icono}
    </IconButton>
  )
}
